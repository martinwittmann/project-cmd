# TODO

## Borg backup
Allow projects to easily use borg backup.

## Completions
- Make project run completions aware of project set via the -p option.
- Add completions for global scripts and allow defining completions for custom scripts.


## Script definitions
Currently each script file needs to be created/copied to make it available for the project.
This makes it simple and easy to check if a script exists and executing it.

- It's quite a bit of manual work to set up for each project.
- When a newer version of a widely used script is available it needs to be copied to each project.
