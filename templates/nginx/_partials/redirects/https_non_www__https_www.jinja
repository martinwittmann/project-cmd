# SSL non-www, redirect to SSL with www.
server {
  server_name {{ domain }};
  listen 443 ssl;
  listen [::]:443 ssl;
  http2 on;

  access_log ofd;
  {% include '_partials/error_log.ninja' %}
  {% include '_partials/ssl.ninja' %}
  {% include '_partials/acme_challenge.ninja' %}

  location / {
    return 301 https://www.$server_name$request_uri;
  }
}